# WorkBuddy 界面长啥样？三块区域带你五分钟不再迷路

你第一次打开 WorkBuddy，左边一栏、中间一片空白、右边又一栏，光标在输入框里一闪一闪。你想让它帮你干活，又怕点错了它乱动你电脑里的文件；想先把界面研究清楚再动手，可满屏的按钮看得人发懵。其实界面没那么复杂，你只要认准三块区域，剩下的边用边熟。

先记住这个全局：WorkBuddy 主界面可以理解为三个区域，左边管任务，中间下指令、看它干活，右边看产物和文件。下面这张图先给你一个整体印象。

![](/workbuddy/03-interface/assets/001_image_MuLCbdPyDo.png)

## 三块区域分别在忙什么

把这三块搞明白，你基本就不会在界面里迷路了。

| 区域 | 主要用途 | 使用时重点检查 |
|-|-|-|
| 侧边栏 | 新建、搜索、切换和管理任务 | 是否进入了正确任务 |
| 对话区 | 描述需求、补充信息、确认计划 | 目标和约束是否完整 |
| 结果区 | 查看产物、全部文件、变更与预览 | 文件名、路径和改动是否符合预期 |

你可能会注意到侧边栏里有「任务」和「工作空间」两个入口。它们的区别只在于你有没有设置「工作空间」目录。「工作空间」是 WorkBuddy 为了管理任务而设置的目录，每个任务都有一个对应的目录空间，任务可以在目录空间内进行操作。若未设置，它会在默认安装的目录下执行任务，对话则保存在「任务」目录下。

## 为什么要把工作目录隔开

这不只是整理习惯，也是一道安全边界。你把发票、周报、客户材料全堆在一个大目录里，它就更容易读错、改错，甚至把 A 客户的信息串到 B 客户的活里。推荐你按任务建目录空间，活与活之间互不干扰。

你还能对目录空间的权限做设置。当开启「允许完全访问」后，智能体可以读写授权目录外的文件——这个选项请谨慎使用，优先按任务限定目录，别一上来就全放开。

![](/workbuddy/03-interface/assets/002_image_DtASbQcrto.png)

## 三种工作模式，你该用哪一个

WorkBuddy 给你三种工作模式，区别在「它动不动你的文件」。

| 模式 | 中文界面 | 能做什么 | 适合场景 |
|-|-|-|-|
| Ask | 问一问 | 问答、理解和查看，不修改文件 | 先了解资料、确认需求 |
| Craft | 做一做 | 可直接操作本地文件、运行代码及系统指令 | 路径清楚、风险较低的任务 |
| Plan | 想一想 | 先生成计划，确认后再执行 | 多步骤、跨系统、重要文件任务 |

![](/workbuddy/03-interface/assets/003_image_W7VqbwVeJo.png)

新手建议从 Ask 或 Plan 起步：Ask 只问不动，最安全；Plan 先给你看方案，你点头它再干。等你对它的能力有把握了，再在合适的任务上用 Craft。

## 挑一个顺手的模型

默认是自动模式，你也能指定想用的模型。不同模型积分消耗不同，你别只看名气，要看你的活适合谁。

![](/workbuddy/03-interface/assets/004_image_OzThbMYn5o.png)

| 任务特征 | 优先关注 |
|-|-|
| 大量文本与长资料 | 上下文长度，比如 100 万 tokens（1M） |
| 图片与截图 | 需要有视觉理解能力 |
| 高频简单任务 | 优先选择响应速度快、成本较低的模型 |

## 看腻了默认样子？换肤就行

默认界面看久了想换换模样？可以试试本站的 [WorkBuddy 皮肤工坊](/workbuddy/skins/)：12 款现成主题皮肤（暗色、亮色都有）一键下载导入就能换上，也提供 skin-generator 教程，给 AI 一张参考图就能生成你的专属皮肤。

## 新手常见问题

**三个区域我老是搞混，最该先盯哪块？**
先把中间对话区用起来——你在那下发任务、看它怎么想，结果区的文件和预览也跟着刷新。侧边栏只是切换和新建，不急。

**工作空间一定要建吗？**
不一定。你不设，它就用在默认目录里执行的「任务」模式。但如果你手头有好几个不同项目、还涉及客户资料，建独立工作空间能让活与活之间不串门，也更安全。

**Ask、Craft、Plan 第一次用选哪个？**
拿不准就选 Ask（只问不动）或 Plan（先看方案再执行）。Craft 会直接动你本地文件，新手第一单先用更稳的模式，等心里有数了再放开。

**模型选自动还是手动？**
第一单用自动就够了，它自己挑合适的。等你发现某个活总跑偏、或者成本太高，再按上面的表格手动换模型。

---

下一步：发你的第一单——[快速完成第一个 WorkBuddy 任务 →](/workbuddy/04-first-task/)
